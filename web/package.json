{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "clear && export DEBUG=vite:proxy && vite",
    "build": "rm -rf ../app/html && tsc && vite build",
    "preview": "vite preview",
    "mock": "clear && node ./public/mock-server.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "sass": "^1.85.0",
    "typescript": "~5.6.2",
    "unplugin-auto-import": "^19.0.0",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "axios": "^1.7.9"
  }
}